# EXP-002 rerun result — 2026-08-08

**Verdict: falsified.** After the sandbox repair, the sealed-spec checker
(`scripts/check_experiment_spec.py`) exited 1 against
`experiments/EXP-002-forced-replay-and-route-precision.json` (spec sha256
`ff97deb7…`, binding intact): 3 of 13 predeclared rules failed, 0 unevaluable,
report usable (`unusable: false`), falsified hypothesis: **H3**.

The stored verdict at `.atomic/exp002-checker-verdict.json` is again
head-truncated at 6,000 bytes. Re-running the checker against the same sealed
spec and report reproduced the identical result (exit 1, 3/13 failed).

## Sandbox health — this time it worked

Unlike the 2026-08-08 first run (every LXD launch timed out; live capture
0/123; the run measured nothing), this rerun's infrastructure was healthy:

- LXD 6.9, image `grove-python-base`, profile `grove-sandbox`, network
  detached. Probe: launch 3.30 s in 1 attempt, exec 0.18 s, delete 5.25 s.
  No launch timeouts, no queue delay.
- Live capture: 123 tasks, **94 passed, 29 failed** — every failure is a
  genuine candidate failure ("candidate exited with status 1" ×13, "one or
  more hidden cases failed" ×16), all on `path_*` tasks. No sandbox
  infrastructure errors anywhere in the report.
- Verifier confirmed **20 of 20** canonical training proposals
  (`verified_corrections = 20`); one expert (`expert_55951989acbe`) trained
  (34.8 s) and admitted.
- Integrity: evaluation store clean (3/3 records verified), ledger clean
  (191/191), report bound to the sealed spec, every manifest binding intact.

The run measured what it set out to measure.

## Decision rules

Run: `/srv/storage/grove/evaluations/exp002.json`, primary arm,
2026-08-08 02:25 → 04:46 UTC, base
`mlx-community/Qwen2.5-Coder-1.5B-Instruct-4bit`, canonical corrections.

| Rule | Hyp | Threshold | Measured | Outcome |
|------|-----|-----------|----------|---------|
| D1 heldout_target_rate | H1 | >= 0.75 | 0.75 (3/4 held-out) | pass |
| D2 regression_rate (routed) | H2 | <= 0.0 | 0.0 (0/94) | pass |
| D3 forced_replay_measured | H3 | == true | true | pass |
| D4 forced_regression_denominator | H3 | >= 50 | 94 | pass |
| D5 forced_regression_rate | H3 | <= 0.0 | **0.4894 (46/94)** | **fail** |
| D6 forgetting_claim | H3 | == adapter_intrinsic | **router_shielded** | **fail** |
| D7 route_recall | H4 | >= 0.9 | 1.0 (3/3) | pass |
| D7a route_positive_source | H4 | == heldout_forced_pass_oracle_free | as required | pass |
| D7b route_probe_metadata | H4 | == oracle_free | oracle_free | pass |
| D8 route_false_positive_rate | H4 | <= 0.0 | 0.0 (0/94) | pass |
| D9 provenance_gaps | — | subset of {models.base.aggregate_sha256, worker.model_manifest_sha256} | 5 undeclared gaps: worker.checkout.{dirty, revision, status_sha256, tree, worktree_sha256} | **fail** |
| D10 provenance.source.dirty | — | == false | false | pass |
| D11 run_manifest_sha256 | — | exists | present | pass |

Key values, stated plainly:

- **Forced-regression denominator: 94** prior-passing replay tasks (spec floor
  50). This is a real cohort: 94 of 123 live-capture tasks passed at baseline.
- **Forced-regression rate: 46/94 = 0.4894** against a threshold of 0.0. With
  the adapter forced on, it breaks 46 tasks the base model passed — spread
  across lists, strings, dicts, and arithmetic (`reg_dedupe`, `reg_uppercase`,
  `reg_dict_invert`, `reg_add_two`, …). Forced replay pass rate: 0.5106.
- **forgetting_claim: `router_shielded`** (required: `adapter_intrinsic`). The
  report itself records that the observed zero routed regression (D2) is a
  property of the router keeping base traffic away from the adapter, not of
  the adapter retaining prior skills.

## Reading

H3 — "the adapter itself forgets nothing" — is **falsified**. The adapter
catastrophically forgets roughly half the prior-passing cohort; the clean D2
number exists only because the router (0.0 false-positive rate, D8) never
routes base traffic to it. That is a scientific result of this experiment, not
a bug to fix, and no threshold should be loosened to accommodate it.

D9 failed on substance, independent of H3: the worker still reports five
checkout-provenance gaps beyond the two the spec declared uncloseable. Even
the passing rules therefore rest on partial provenance.

## What remains unproven

- **Intrinsic retention (H3): falsified here**, not merely unproven.
- H1, H2, and H4 rules passed, but on thin evidence: one expert, one task
  family, 4 held-out plasticity examples (D1 sits exactly at its 0.75
  threshold), and 3 routing positives. These support, not establish, their
  hypotheses.
- The Grove thesis as a whole is **not proven** by this run — and cannot be by
  any single experiment. What this run shows is narrower: the system's
  no-regression behavior is currently router-shielded, so any thesis claim of
  safe accumulation depends on router precision holding as experts multiply.
  That scaling question (and EXP-003's correction-source A/B) is untested.
- Complete provenance remains unmet (D9).
