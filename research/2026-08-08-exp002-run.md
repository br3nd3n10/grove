# EXP-002 run result — 2026-08-08

**Verdict: falsified.** The sealed-spec checker (`scripts/check_experiment_spec.py`)
exited 1 against `experiments/EXP-002-forced-replay-and-route-precision.json`
(spec sha256 `ff97deb7…`, binding intact): 11 of 13 predeclared rules failed,
0 unevaluable, report usable (`unusable: false`).

Note: the stored verdict at `.atomic/exp002-checker-verdict.json` is
head-truncated at exactly 6,000 bytes. Re-running the checker against the same
sealed spec and report reproduced the identical result (exit 1, 11/13 failed).

## What ran

- Report: `/srv/storage/grove/evaluations/exp002.json`, primary arm,
  2026-08-07 21:36 → 2026-08-08 00:40 UTC.
- Base model `mlx-community/Qwen2.5-Coder-1.5B-Instruct-4bit@b3252a2f`,
  canonical correction source, cycle `cycle_9e8b23934126`.
- Every LXD sandbox launch timed out (`launch grove-python-base … --profile
  grove-sandbox`). Live capture: 0 of 123 tasks passed, all with "sandbox
  infrastructure error". Baseline cohort pass rates: 0.0 across the board.
- All 20 canonical training proposals were rejected (verifier could not confirm
  any of them in the sandbox), so `verified_corrections = 0`. Both clusters
  (`core_python`, `escaped_path`) were skipped: "not enough verifier-backed
  corrections to train safely". 0 candidates trained, 0 experts admitted,
  `experts: []`.

## Decision rules

With an empty expert cohort, every per-expert rule fails by predeclared
construction ("no values to compare"); none was marked unevaluable.

| Rule | Hyp | Threshold | Measured | Outcome |
|------|-----|-----------|----------|---------|
| D1 heldout_target_rate | H1 | >= 0.75 | absent (no experts) | fail |
| D2 regression_rate | H2 | <= 0.0 | absent (no experts) | fail |
| D3 forced_replay_measured | H3 | == true | absent (no experts) | fail |
| D4 forced_regression_denominator | H3 | >= 50 | absent (no experts) | fail |
| D5 forced_regression_rate | H3 | <= 0.0 | absent (no experts) | fail |
| D6 forgetting_claim | H3 | == adapter_intrinsic | absent (no experts) | fail |
| D7 route_recall | H4 | >= 0.9 | absent (no experts) | fail |
| D7a route_positive_source | H4 | == heldout_forced_pass_oracle_free | absent | fail |
| D7b route_probe_metadata | H4 | == oracle_free | absent | fail |
| D8 route_false_positive_rate | H4 | <= 0.0 | absent (no experts) | fail |
| D9 provenance_gaps | — | subset of {models.base.aggregate_sha256, worker.model_manifest_sha256} | 5 undeclared gaps: worker.checkout.{dirty, revision, status_sha256, tree, worktree_sha256} | fail |
| D10 provenance.source.dirty | — | == false | false | pass |
| D11 run_manifest_sha256 | — | exists | present | pass |

Key requested values, stated plainly:

- **Forced-regression denominator:** never recorded — no expert existed to
  replay against. The spec demanded >= 50 prior-passing tasks; the run captured
  a 123-task replay ceiling (103 regression + 20 train), but 0 passed live
  capture, so no prior-passing cohort exists.
- **Forced-regression rate:** never recorded (threshold <= 0.0).
- **forgetting_claim:** never recorded (required value `adapter_intrinsic`).

D9 failed on substance independent of the empty cohort: the report carries five
provenance gaps beyond the two the spec declared uncloseable in advance.

## What remains unproven

Everything this experiment set out to test. No hypothesis received a
substantive measurement:

- H1 (held-out target fix rate) — untested; no expert was trained.
- H2 (routed replay does not regress) — untested.
- H3 (adapter-intrinsic no-forgetting under forced replay) — untested; the
  central claim EXP-002 was designed for was never exercised.
- H4 (route recall / zero false-positive routing) — untested.

Under the predeclared rules this run is a falsification, not an infrastructure
excuse: the rules deliberately treat an empty expert cohort as failure. At the
same time, a run that measures nothing confirms nothing — this result says
nothing in favor of the Grove thesis, and the thesis remains unproven. The
provenance-gap rule (D9) also failed and would have failed even with a full
expert cohort. Timing attestation is self-consistent only; the seal supports no
preregistration claim.
