# EXP-003 A/B: self-repair vs canonical corrections — checker verdict: UNUSABLE (exit 2)

Date: 2026-08-08. Spec: `experiments/EXP-003-correction-source-ab.json` (sealed,
sha256 `402c5745…b24d22`, binding intact, 15 rules, 2 hypotheses).
Reports: primary `/srv/storage/grove/evaluations/exp003-self.json` (arm `primary`,
source `self-repair`), control `/srv/storage/grove/evaluations/exp003-canonical.json`
(arm `control`, source `canonical`). Checker rerun confirmed exit 2:
`rules_total 15, rules_failed 0, rules_unevaluable 15`.

## Did both arms measure? Yes.

This was not an infrastructure failure. In both arms the sandbox probe passed
(image `grove-python-base`, LXD 6.9, launch ~3.3 s, exec OK, no network) and the
worker reported `status: ok`. The primary arm's zero yield is a measurement: the
worker made all 60 generation calls (3 attempts × 20 failures) and the verifier
rejected every candidate — 11 tasks failed with candidate exit status 1, 9 failed
hidden cases.

## Worker provenance (D9-class)

No D9-class gaps in either arm. Both reports carry:

| Field | Primary | Control |
|---|---|---|
| `run_manifest_sha256` | `5a50f47d…50ccc7` | `904d3b24…958493` |
| Worker host | grove-worker-1 | same |
| Checkout | `8f7f187d`, clean | same |
| Framework | mlx 0.32.0 / mlx_lm 0.31.3 / py 3.14.3, arm64 | same |
| `model_manifest_sha256` | `25d34506586c…` (9 files) | identical |
| `provenance_gaps` | `["models.base.aggregate_sha256"]` | same |

The only gap is the declared-uncloseable control-host one, exactly the set D7
permits. Both arms prove they loaded the same model files
(Qwen2.5-Coder-1.5B-Instruct-4bit @ `b3252a2f`).

## Verified-correction yields per arm

- Primary (self-repair): **0 verified corrections** of 20 targeted failures.
  No training data reached the trainer, so no expert was trained.
- Control (canonical): **20 verified corrections** of 20; one expert trained and
  admitted (`expert_d24844767e81`).

## Per-source correction comparison (same 20 failures, both reports agree)

| Source | Verified / failures | Rate | Gen. calls | Mean attempts |
|---|---|---|---|---|
| canonical-reference-v1 | 20 / 20 | 1.00 | 0 | 1.0 |
| self-repair-v1 | 0 / 20 | 0.00 | 60 | 3.0 |

Coverage: all 20 tasks fixed only by canonical; none by self-repair; none by
neither. Self-repair rejections: 11 × "candidate exited with status 1",
9 × "one or more hidden cases failed".

## Decision rules

The checker marks **all 15 rules unevaluable** — none passed, none formally
failed. Once the pairing rule D6d found no primary value, the checker refused to
grade the run: "arms could not be paired: rule D6d: primary delta path
`experts[*].metrics.heldout_target_rate` is absent." Measured values below are
what the reports contain, not graded outcomes.

| Rule | Hyp | Requirement | Measured | Note |
|---|---|---|---|---|
| D1 | – | primary source == self-repair | `self-repair` | matches |
| D2 | – | comparison sources == both | both present | matches |
| D3 | H1 | self-repair verified_rate ≥ 0.5 | **0.0** | far below bar; ungraded |
| D4 | H1 | provenance source == self-repair | `self-repair` | matches |
| D5 | H2 | primary admits ≥ 1 expert | **0** | below bar; ungraded |
| D5c | – | control source == canonical | `canonical` | matches |
| D6 | H2 | primary heldout_target_rate ≥ 0.75 | absent (no expert) | nothing to compare |
| D6c | H2 | control admits ≥ 1 expert | 1 | meets count |
| D6d | H2 | heldout delta (primary−control) ≥ −0.1 | primary value absent | pairing failure; drove exit 2 |
| D6e | H2 | forced-regression delta ≤ 0.0 | primary value absent | same |
| D7 | – | gaps ⊆ {models.base.aggregate_sha256} | exactly that set | matches |
| D8 | – | pairing_key exists | present | control expert carries it |
| D9 | – | run_manifest_sha256 exists | present, both arms | matches |
| D10 | H1 | training reused comparison proposals | true | matches |
| D11 | – | worker model_manifest_sha256 exists | present, identical | matches |

## Expert admission

- Primary: no expert admitted (D5 count 0). Nothing trained.
- Control: `expert_d24844767e81` admitted. Metrics: heldout_target_rate 0.75
  (exactly the D6 bar), target fix 0.0 → 0.9, replay routed 1.0,
  route precision 1.0, forced_regression_rate 0.489 (46/94, router-shielded —
  consistent with EXP-002's rerun result).

## What this run establishes, and what remains unproven

The checker returned **unusable**: this run formally supports neither
confirmation nor falsification of H1 or H2. The sealed protocol could not pair
the arms because the primary produced no expert, and the checker grades nothing
in that state. That said, the raw measurements are honest and bleak for H1:
Qwen2.5-Coder-1.5B-4bit produced zero verifier-approved self-repairs in 60
attempts over failures the canonical references fix 20/20.

Unproven after this experiment:

- H1 — that the deployed model self-repairs at a useful rate. Measured 0.0
  against a 0.5 bar, but ungraded here.
- H2 — that a self-repair-trained expert matches canonical admission. Never
  tested: no self-repair expert existed to compare.
- The Grove thesis overall. This single unusable run moves it in neither
  direction, and no prior run proves it either.

Timing: the seal is self-consistent but carries no external timestamp; the
report cannot support a preregistration claim (checker `timing.status:
unverified`, non-blocking).
